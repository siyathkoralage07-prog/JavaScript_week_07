//HELLO TO JavaScript!!!
console.log("hello, world");

// sending alerts
alert("hi icet :")

// no need to use a semi-colon to define lines
console.log("hi.....hi.....hi");

//Assigning Variables(in js the default setting for variables is non-type_strict, AND all variable names are CASE SENSITIVE soooo (eg. "name" and "NAME" are two different variables))
let name = "saman";
let age = "20";
let afterAL = true;
let customer = []; // an ARRAY
let cashier = {}; // an OBJECT

console.log(typeof name);
console.log(age);
console.log(afterAL);
console.log(typeof customer);
console.log(cashier);

